const initialState = {
    'Понедельник': [
        {
            time: '8:30 - 10:05'
        }
    ]
}

export default function footer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}
